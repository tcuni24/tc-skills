# 实例：TC-HNU-AT-100K 探针设计报告改版（2026-09-16）

项目：节节麦 / AK58 D 基因组来源判别探针，A、B 两版并列交付。
客户反馈：报告对非专业客户不友好，太复杂。客户真正关心的两个问题：
1. 节节麦 / AK58 各有多少比例的 1 Mb 区间能确认身份
2. 不同类型的探针各起什么作用，分析时怎么用

文件：
- 原内容层 `workflow/templates/at100k_delivery_content.py`（10 章，保留）
- 新内容层 `workflow/templates/at100k_delivery_content_simple.py`（6 章，正文 4 章 + 文件 + 折叠附录）
- 渲染器 `workflow/scripts/14_delivery_report.py` 加 `--content` / `--out-dir` 与 `collapse` 块类型
- 输出 `delivery/20260902/TC-HNU-AT-100K-Probe-simple/`

## 诊断时数出来的证据

| 症状 | 证据 |
|---|---|
| 问题一被 7 种口径回答 | 节节麦侧 / AK58 侧 / 任一侧 / 双侧 / 成对层 / 10 kb 网格密度 / 节节麦物理坐标口径（分母还从 4,038 变 4,025）；核心数字在页眉、01、02、05、08 章重复 6 次 |
| 问题二叠了三层分类 | probe_type（3 种）、locus_type + readout（5 × 4 英文枚举）、probe_class（A–D）、probe_level / hairpin_level；probe_class 表用四行说「不代表什么」 |
| 两版选择挤占核心问题 | 「为什么有两版」讲了 4 遍；05、06 章 16 张图全部 A/B 各一张，06 章 8 张全是内部 QC |
| 术语进正文 | ρ、ρ₀/ρ₁、分离度、D_min、NoCall、HMM、变点检测、PAV、N50、mappability，及十几个列名 |
| 防御性论证 | 「探针数量有限不是设计保守」「为什么没有要求 ≥2 份材料支持」「为什么模拟验证回答不了这个问题」「这不是概率问题，是确定会发生的」 |
| 免责前置 | 01 章「阅读本报告前需要知道」四条限制排在答案之前 |

## 前后结构

| 原 | 新 |
|---|---|
| 01 结论摘要（结论 + 两个重点 + 四条须知 + 六行指标表） | 01 一页结论（一句话结论 + 2×2 覆盖表 + 三张探针卡片 + 唯一要确认的事） |
| 02 背景与原理（问题一、问题二混在一章，5 张表 + 3 张 SVG） | 02 问题一（定义 + 新画「区间怎么算」示意图 + 含盲区行的表 + 2 张全基因组图 + 注意）|
| | 03 问题二（流程图 + 探针类型表 + 新画「成对看比例 / 单侧看有无」双栏图 + 判读表 + 四步） |
| 03 两版怎么选（决策表 + 论证数字 + 三段 note） | 04 两版怎么选（决策表 + 对比表 + 对比图 + 请反馈） |
| 04 流程漏斗 | 附录 B |
| 05 分布（8 张图 + 6 口径表） | 02 章留 2 张，其余 6 张 + 5 口径表进附录 A |
| 06 质量（8 张图 + 「为什么不收紧」） | 附录 C，只放 A 版 4 张 |
| 07 模拟验证 | 附录 D，stat_table 从 5 行减到 4 行，去掉 ρ / D_min |
| 08 限制（L1–L11） | 影响操作的 4 条散入正文，其余进附录 E（8 行，去掉编号） |
| 09 文件 | 05 文件 |
| 10 术语表（19 条，含列名） | 附录 F 字段对照（11 行）+ 附录 G 术语表（13 条，纯概念） |

## 具体的合并与改名

- AT_ONLY + HALF_AT → 「单侧位点 · 只有节节麦探针」（两者判法相同）；HALF_AK → 「单侧位点 · 只有 AK58 探针」
- AT-P / AK-P / SR-P → 节节麦探针 / AK58 探针 / 内参探针（代码只在附录 F）
- 「覆盖」→「能确认身份的区间比例」；「成对位点层」→「只算成对位点」（附录）
- 新增「两种都不能确认（盲区）」行：4,038 − 任一侧 = 291 / 262
- 四步流程去掉 HMM / 变点检测 / NoCall，保留「连续 3 个以上位点指向同一方才算一段」

## 检查结果

- 正文（附录前）约 4,500 字，原报告约 1.8 万字
- 正文术语 / 列名命中 0；缺图 0；13 张内嵌图；7 个折叠块
- 原目录报告与 SHA256SUMS 时间戳未变
- 未做：新图（全部复用现有 PNG）、像素级验证（无无头浏览器）
